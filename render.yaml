services:
  - type: web
    name: goldgpt-app
    env: python
    buildCommand: "pip install -r requirements.txt && npm install && npm run build"
    startCommand: "python app.py"
    envVars:
      - key: PYTHON_VERSION
        value: 3.9.0